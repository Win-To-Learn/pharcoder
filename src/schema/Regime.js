/**
 * Regime.js
 *
 * Represents a set logical set of users (school, class, guests, demo subscribers, etc.)
 */
'use strict';

var Regime = function (config, parent) {
    this.parent = parent;
};

module.exports = Regime;
